apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cloudflared-tls
  namespace: cloudflare
spec:
  dnsNames:
  - "*.kbushby.com"
  - "*.homelab.kbushby.com"
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: cloudflare-ca-issuer
  secretName: cloudflared-tls