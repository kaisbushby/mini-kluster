apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jenkins-oidc-secret
  namespace: jenkins
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    # name of the generated secret
    name: jenkins-oidc-secret
  data:
  - secretKey: secret # Name of the element in the generated secret
    remoteRef:
      key: oidc/jenkins  # The path of the secret, relative from the secrets path
      property: secret  # name of the attribute to access