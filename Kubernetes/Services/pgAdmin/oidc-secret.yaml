apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pgadmin-oidc-secret
  namespace: pgadmin
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    # name of the generated secret
    name: pgadmin-oidc-secret
  data:
  - secretKey: secret # Name of the element in the generated secret
    remoteRef:
      key: oidc/pgadmin  # The path of the secret, relative from the secrets path
      property: secret  # name of the attribute to access
